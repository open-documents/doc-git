
使用账号和密码可以通过 `https://` 协议来连接到 Git repositories，但是有时只是想简单地 `clone` public项目，此时甚至可以不登录来完成此事，这种情况下可以使用

## 创建SSH Key

参考文档：`https://git-scm.com/book/en/v2/Git-on-the-Server-Generating-Your-SSH-Public-Key#_generate_ssh_key`

